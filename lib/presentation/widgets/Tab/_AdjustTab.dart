import 'package:flutter/widgets.dart';

/// Legacy placeholder kept so older references to this file continue to resolve
/// while the active app uses the newer controls in `bottom_controls.dart`.
class LegacyAdjustTabPreview extends StatelessWidget {
  const LegacyAdjustTabPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
